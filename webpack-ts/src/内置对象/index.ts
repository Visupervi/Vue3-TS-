(() => {
  /**
   * ECMAScript内置对象
   */
  let b: Boolean = new Boolean(true);
  let n: Number = new Number(1);
  let s: String = new String("123");
  let d: Date = new Date();
  let e: Error = new Error("12312313");

  /**
   * DOM与BOM内置对象
   * Window, Document, HTMLElement, DocumentFragment
   * Event
   * NodeList
   */
})()
